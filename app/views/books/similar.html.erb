<%= turbo_frame_tag 'similar' do %>
  <div style="display: flex; flex-direction: row; flex-wrap: wrap">
    <% @books.each do |book| %>
      <%= render partial: 'book_preview', locals: { book: book } %>
    <% end %>
  </div>
<% end %>
